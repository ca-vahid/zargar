# C6 completion and parallel Practice launch

User authorized implementation, testing, merge, coordinated deployment and readiness for September 18 while away. Practice only. C2 remains sealed; no validation-period method outcomes were inspected or tuned.

## Implementation

Bars now carry `provider` independently of `source`. Alpaca stream/history and Yahoo poll/history stamp their own provider. A later Yahoo or unidentified exchange observation cannot replace an Alpaca observation. The same policy applies in the aggregator, batched flush, SQL upsert and Team2's private tape. Within a provider the established correction/zero-volume policy remains; an Alpaca replacement never borrows Yahoo volume. Content hashes include provider. Old unknown rows stay unknown until actually refetched.

`techniques.team2.canonical_provider` defaults off. With `alpaca` selected, Team2 planning, warm-up, pre-market, live decisions and research exclude other providers. New plans require twelve valid canonical warm-up sessions and stamp `inputProvider`. Warm-up is saved in a content-addressed immutable snapshot shared across books. Live/restore/replay load those exact rows. Missing/corrupt snapshots fail rather than substituting another seed. Decision journals record the current input hash and indicator state; later corrections remain revisions, not retroactive orders.

`python -m zargar.tools.team2_c6 --apply --migrate --end 2026-09-17 --out <runtime-log-path>` uses the existing runtime configuration without starting another engine. It archives and verifies SPY/QQQ/IWM before-images, fetches Alpaca SIP extended-hours data directly (no Yahoo fallback), applies provider precedence, records coverage and changed-row examples, then verifies snapshot/indicator identity. Historical plan decisions are retained. Additive schema uses the normal `db.create_all`/`_ensure_columns_sync` path.

The agreed sampled experiment thresholds are checked by engine jobs every thirty minutes from 09:30 through 16:00 ET, independent of agent availability. Sizing uses $800 and C1 $1,000 sampled peak-to-trough drawdown. State retains starting equity/high-water mark across days/restarts. Cash includes fees; held options use fresh bids. Missing marks are unknown and do not update the high-water mark. Breach persists a book pause; it does not change sizing or resume automatically. Protective exits and existing risk limits remain active. These are sampled review thresholds, not guaranteed loss caps.

## Verification and operations

The broad suite exposed a fixture problem: `test_team2_runner.rig` installed no-network hooks after engine startup. At night, scheduler jobs could fetch real bars into synthetic 570-dollar SPY fixtures, producing real 766-dollar levels and changing warm-up hashes. Isolation now precedes startup and the scheduler is disabled only in that manual-driver fixture. This is a reproduced contamination path, not an attribution of every earlier intermittent failure.

Exact results, dataset version and activation receipt are appended after verification. No evidence record is accepted from row counts alone. Historical data inspection is input-only; original versus corrected history are distinct evidence, and old live source provenance cannot be invented retrospectively.

## Measured input evidence

Dataset `2a345877d5448459a3ff9c478eb4ea6713325d41271ecadd7870297ab1f84b75`: 83,445 direct Alpaca SIP minutes, August 3 through September 17. All canonical RTH dates have 390 minutes per symbol. Twelve-session warm-up rows: SPY 10,464; QQQ 10,924; IWM 9,336. Each restored snapshot matches its saved input hash and indicator sequence. Before-images are retained in `team2_tape_snapshots`; the manifest records their IDs. Existing OHLCV changed at 9 SPY, 16 QQQ and 2 IWM minutes. Noncanonical rows were retained in the shared table and excluded from Team2 canonical reads.

Historical ruling: the prior generic `exchange` label cannot retrospectively identify providers, and original F119 decision-time input sets were not saved. Do not claim a unique historical ATR cause from the current table. The code exposed two concrete input divergence paths: cross-provider replacements and pre-open merges retaining an already-present private-tape row while the bank accepted a correction. Both are repaired; prospective immutable warm-up and per-decision hashes establish an auditable common input path. Old experiments/backtests retain their exploratory labels; the accepted C6 record applies to the new prospective cohort, not retrospective certification of lost evidence.

Checks so far: 30 initial focused tests; 40 migration/private-tape/warm-up checks; broad run 318 passed with four fixture contamination failures; after moving isolation before engine startup, the 43-test affected selection passed, including all four failed cases and sampled monitoring. Final combined-tree results and deployment receipt follow.

Final merged-tree acceptance: 75 focused canonical/provider, migration, warm-up, pre-market, experiment isolation and feed tests passed. Frontend production build and release metadata check passed at 0.8.16, retaining Cartel 0.8.15 changes. Test database released before coordinated deployment.


## Final deployed receipt and activation GO

- PR215 merged canonical inputs; PR216 registered the observation-state setting after preflight caught that the mocked settings fixture had not exercised key validation. The new real SettingsService save/reload regression and all nine C6 tests pass. The v0.8.17 production build passes.
- Coordinated deployment with the Cartel task: healthy v0.8.17, build `783be1a14d1071b74701d49328611f5301de59b2`. Guarded restoration verified 59/59 armed plans, 4/4 managed positions and 27/27 resting orders before experiment activation. Two after-hours restarts were needed because the setting registration was caught after the first restart had begun; neither activated the experiments prematurely.
- September 18 Practice activation is GO under the user's overnight authorization. Control, Sizing 0.5 and C1 each start at $10,000. All nine plans are armed in auto, three symbols per book. Three old flat baseline plans were retired through the verified force-replan path; the original Team2 Practice book and its $9,421.19 history remain intact.
- Every plan stamps `inputProvider=alpaca`; all nine actual replay calls matched their immutable warm-up hashes. Shared baseline remains size_full=1, pm_range, both room knobs zero, key_levels off, fresh option quotes required. Only the intended per-book override differs. No C2 result was read or retuned.
- Read-only receipt: `C:/Cursor/zargar/logs/team2-ready-20260918.json`, READY, zero blockers. Input verification: `team2-plan-input-check-20260918.json`. The tool's static phrase 'pending review GO' is superseded by the explicit GO journaled under `techniques.team2.experiment_observation.approval`, including the session, build and receipt reference.
- Runtime logs confirm nightly planning, 09:25 ET pre-open completion and all fourteen half-hour experiment checks from 09:30 through 16:00 ET registered. Persistent high-water state begins at $10,000; sampled $800/$1,000 thresholds pause the corresponding book, preserving exits. The coordinated Cartel task's 05:35 PT pre-open check includes these nine arms, canonical snapshots and job registrations.
- A second read-only canonical audit reproduced dataset `2a345877d5448459a3ff9c478eb4ea6713325d41271ecadd7870297ab1f84b75` exactly, with zero changed minutes. This certifies the new prospective input cohort; historical F119 source attribution remains explicitly limited, and prior research is still exploratory.

Active development folder: `C:/Cursor/zargar-codex/.cache/team2-c6-completion`, branch `codex/team2-c6-completion`. Primary checkout and other desks' worktrees preserved. Deployment was performed by the coordinated Cartel task through the shared guarded door. No real-money books were enabled. Overnight readiness is verified; actual 09:25 pre-open data and next-session profitability can only be observed when that session arrives.
