# Release closure: Cartel brief first package

User authorized the reviewer to finish corrections, push/deploy, and prepare the
next session. R1 restoration now ensures the underlying subscription through the
canonical engine path, bounded to20 seconds; failures persist as control coverage
errors and cannot interrupt protective restore. R6 cumulative fills now come from
cutoff-filtered executions. Order rows updated after cutoff expose unknown request/
status instead of leaking later state. The end-to-end fixture clock and async
selection wait were corrected; missing mock tracking support was supplied.

Selection/ranking are the proposed active Practice changes:
`diverse_liquidity_v1` and `executable_cost_v1`. Keep the existing15m cadence and
volume grid off; non-15m research compatibility and quote observation timestamp
work remain F4 activation prerequisites. Do not change Live or other techniques.
The $1m experiment book/$25k plan budget and protective gates remain unchanged.

Focused verification and deployment/arming results are reported with the release
handback. Earlier review files describe their pinned commits, not current status.
No claim of profitability follows from code acceptance or new arms.
