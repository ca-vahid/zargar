# Deterministic entry v1: tracker-parity review

Reviewed code: `758ccfb8e0a456d1df534f9e91a35e16f47cb352`, isolated checkout `C:/Cursor/zargar-codex/.cache/em-deterministic-final`. Primary reviewer checkout remains `C:/Cursor/zargar-codex`, branch `codex/zargar-development`. This review made no runtime, setting, order or production-code changes. No database tests, engine starts or model calls were made by this reviewer.

**The pure decision implementation needs three corrections before acceptance as a behavior-preserving removal of the fire-time LLM.** Each is reproduced using the actual `TriggerTracker`; these are not requests for new strategy filters. The supplied regression file is `test_codex_deterministic_rule_parity.py`. Direct Python executions reproduced all three; the parent reviewer owns the coordinated pytest run.

The main implementation direction is correct: setup rules are technique-owned and pure; the fired branch comes from tracker events, touches retain T4.2 without requiring a reclaim candle, normal breaks require candidate evidence, and range-break/loose-continuation shortcuts disclose their bypasses. Unencoded chart judgments remain diagnostic. The problems below concern new eligibility behavior and evidence identity introduced while wrapping existing rules.

| ID | Severity | Finding and concrete reproduction | Required correction |
|---|---|---|---|
| DR-01 | P2 | `backend/zargar/technique/entry_decision.py:196-198` adds a confirmation-bar window gate. With default thresholds, a valid breakout candidate at 10:29 ET passes the tracker's prime-open gate; three valid follow-through bars finish at 10:32 ET. The unchanged tracker fires, but `evaluate_entry` now refuses `window_ineligible`. `marketstructure/tracker.py:350` gates the candidate; its follow-through branch at lines 400-421 does not require the confirmation bar to remain in that window. | Record the branch's actual window evaluation and preserve its current semantics. For normal follow-through, retain the candidate's accepted window evidence plus the actual confirmation window; do not add a second execution window filter in this latency release. If the team wants that policy change, propose it separately and measure affected trades. |
| DR-02 | P2 | `snapshot_from_tracker` at `entry_decision.py:304` replaces saved entry with `tracker.fill_price`, then lines 173-179 require every target to be ahead of that value. Real default-threshold breakout: saved entry 100, stop 99, targets 101/108/112; follow-through completes at 101.2. The tracker fires, but the evaluator reports `geometry_invalid`. The saved geometry is valid; for a small position's existing TP2 policy the current-close payoff is still `(108-101.2)/(101.2-99) = 3.09R`. Thus this is an added TP1-distance filter, rather than the retained exit-dependent R2 check. | Keep saved planned entry and observed trigger/fill proxy as separate fields. Validate saved geometry against saved entry. Leave current-price feasibility, chase and quantity-dependent exit R:R to the existing downstream checks; do not require every target to remain ahead as a new setup filter. This test asks only for setup-level allowance, never for bypassing downstream controls. |
| DR-03 | P2 | `backend/zargar/technique/arming.py:120` constructs the decision policy from a fresh `self.technique.thresholds()` read rather than the `tr.thresholds` that actually produced the transition. Trackers retain the rule object created by `execution/planrunner.py:1270-1279`. A bounce armed with floor 0.5 fires on observed relative volume 0.7; if the current setting becomes 0.8, the new wrapper refuses and records 0.8 as the supposedly effective threshold, despite the actual tracker having used 0.5. | Freeze the actual tracker rule object and its window-enforcement state with the transition. If settings are intended to affect already armed trackers, define and implement that migration atomically rather than silently judging an old transition under a different rule set. Include the actual confirmation-rule inputs in the threshold identity. |

For DR-03, the current threshold snapshot also omits settings that create the evidence it claims to formalize: `decisive_body_ratio`, `decisive_size_mult`, `max_breakout_wick_ratio`, `range_break_bars`, and `range_break_max_range_mult`. Two actual confirmation policies can therefore share a threshold hash. Include them in the actual-rule snapshot/hash while retaining a single shared tracker implementation.

Acceptance coverage should preserve the three supplied real-tracker regressions, add the short mirrors, retain invalid saved geometry refusal, and verify that subsequent sizing/chase/R2/quote checks can still reject a setup that received an `allow`. Include candidate-ineligible behavior to ensure DR-01 does not weaken the existing window rule. None of these corrections requires a model call or a new quantitative threshold.

The pure module's `plan_current` check does not independently validate session/date/bar identity; it currently checks armed status and nonempty run/trigger IDs. This subreview does not call that a separate blocker because the live runner owns session routing and actual-bar provenance. Its acceptance must be established in the parent runner review, rather than treating the pure check's name as proof of those wider guarantees.
