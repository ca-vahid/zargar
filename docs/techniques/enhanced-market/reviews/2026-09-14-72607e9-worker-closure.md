# EM worker closure at 72607e9

**WF-01 through WF-03 are closed on the reviewed commit. Scoped code/merge GO.** No new EM blocker was found in this bounded rereview. Release integration remains required because newer main changes are outside this tested tree. Trading-performance research proceeds separately.

Reviewed commit: `72607e90c516fd441754455b04ddb9b91dfd7a76`, verified as the remote desk tip during inspection. Includes main through `5fc2852`. Review checkout: `C:/Cursor/zargar-codex/.cache/em-wf-closure`, detached at the reviewed SHA. Primary checkout remains `C:/Cursor/zargar-codex`, branch `codex/zargar-development`. No production code, runtime, settings, orders, backfills or money records changed by this review.

## Independent verification

Sequential command through the isolated test runner:

```powershell
./scripts/test-codex.ps1 tests/test_codex_worker_revision_ownership.py tests/test_em_worker_ownership.py tests/test_codex_worker_followup_boundaries.py tests/test_em_worker_followup.py -q --tb=short
```

**20 passed in 53.60 seconds.** The original six-case and three-case reviewer files match the supplied originals after newline normalization. No competing pytest process was present at start; the disposable `zargar_test_codex` database had no clients before or after this run. No broad suite was independently rerun.

The developer's response document resolves the garbled pasted arming row: it reports 78 reviewer passes, 133 EM/API/Team2 passes, and 29 arming passes plus the previously reported `test_auto_options_one_contract_lifecycle` failure. Those broader numbers and its main-baseline comparison remain team-reported evidence. The load-sensitive restore run is separately disclosed, not counted as a new regression.

| Finding | Closure evidence |
|---|---|
| WF-01: manual board used an old extraction | Both direct reproduction and actual manual API test pass. The board selects a persisted extraction belonging to the current note/revision; stale/missing extraction returns 409 before planning. The board records the selected artifact and revision. |
| WF-02: expired/reassigned lease reached arming | Expiry, reassignment, and source/lease change during awaited arm cases pass. Each attempt has a distinct owner; validity includes owner, fence, expiry and in-progress state. Authorization is checked before planning, arming and publication. |
| WF-03: failure wrote without original ownership | Revision-only failure and stale failure after takeover cases pass. Failure handling requires the original valid job context and makes no fresh claim. Newer work remains intact. |

Actual shared-runner placement was also inspected: `backend/zargar/technique/service.py:1819` forwards the optional callback after its own awaited preparation; `backend/zargar/execution/planrunner.py:1033` awaits it immediately before the first `_armed` registration at line 1035, with no intervening await. EM inherits that runner implementation. Existing callers retain the optional default. This is static verification of the real runner; the added worker test observes a mocked arm boundary, not a real runtime arm.

## Release handoff

1. Integrate the selected current main under the normal release protocol, preserving both desks' shared runner changes, launch-bound build metadata, and the deployment receipt's restoration/health-build/before-inventory fields. Main advanced during this review; for example `c1a47333df99751b678d8fc5114213f64857c4e9` was observed after the earlier `c45ce7e` snapshot. Neither is included merely because `5fc2852` is included. Freeze and record the actual combined release SHA instead of chasing a moving label.
2. Rerun these 20 cases plus affected runner/restore/API/release checks on that exact combined tree in an exclusive test window. The newer main runner delta includes Team2's uncertain-submission reconciliation, so preserve and test that behavior alongside EM's authorization callback.
3. With a clean integration and checks, the release owner can use the existing deployment lock/readiness/restart/verification process. Return the deployed full build SHA and restoration receipt. If integration changes the reviewed EM behavior, return that specific diff for review; a docs-only main advance does not require restarting the entire audit.

This closes the requested worker defect cycle at the code-review level. It does not attest that this build is running, approve a new trading strategy, or apply the separate FIX-01 money repair. Existing source-backfill closure and the scoped non-live FIX-01 decision remain unchanged.

Next EM research remains the [actual trading and missed-setup review](2026-09-14-trading-results-and-missed-setups.md): source continuation definitions and separate exit comparisons with entries/contracts/quantities fixed. No unrelated infrastructure expansion is requested.
