# EM prospective definitions - frozen 2026-09-19, revision 2 (supersedes `PROSPECTIVE-DEFINITIONS-2026-09-19.md`)

Why a new file: the revision-2 review (`reviews/2026-09-18-INTEGRATED-R2-REVIEW.md`, R2-01 and R2-02) and the final-completion goal
(`reviews/EM-FINAL-COMPLETION-GOAL-2026-09-18.md`) found defects in frozen versions BEFORE any validation data was collected. No collector had
been switched on, so no sample is affected. The two earlier files are kept unchanged as the record of what was frozen then. The evaluation
horizon, the fixed research strata and the "what is NOT claimed" section of the 2026-09-18 file carry over unchanged.

## Frozen versions (replacing the 2026-09-19 table)

| Version | What it fixes | Where |
|---|---|---|
| `book-snapshot-v3` / `profit-capture-reducer-v3` (were v2) | the execution ledger is per TRADE INSTANCE (= the entry order id) from the runner's journaled order links: partial fills of one entry combine, different entries never merge, an exit belongs to the entry it is linked to; an unlinked execution, a carry-in, an exit beyond its entry, a symbol conflict or an unknown instrument make the ledger `unreconciled` (unscorable) and nothing is attributed by symbol. The money multiplier comes from the order's security type checked against the OCC identity. Every record's ledger is a bounded full re-read of the session (no execution-time cursor); occurrence time and ingestion time are kept apart; the reducer marks a capture REVISED when a fill that occurred before it became known later, and a revised capture cannot hold the executable peak. Sessions and quote eligibility follow the exchange calendar (holidays held, early closes, DST). The reducer refuses mixed book / session input, sums drops across recorder instances and reports `partial_comparisons_unavailable` instead of `ok` when a comparison is missing | `technique/profit_capture.py`, `profit_capture_runtime.py` |
| `candidate-pricing-v2` (was v1) | gates: contract (OCC identity bound to the candidate: underlying, right, expiry not expired, 0DTE cut-off, metadata conflicts), quote, spread, sizing, budget (net of existing entry reservations), chase, firstSaleR, portfolio. `feasible` ONLY when every gate passes; any unknown = `unknown` with `completeness: partial` and the missing gates listed; never production-equivalent by label | `technique/source_candidate_policy.py` |
| `candidate-chase-v1` (new, research) | the validated executable underlying QUOTE bound (long ask, short bid) is at most 0.25R beyond the entry in the trade's direction. Separate from R. A print is not an executable quote: print-only evidence leaves the chase gate unknown. Production EM has no general underlying chase cap; this difference is declared in every record | same |
| portfolio evidence (new) | the production `RiskGate.evaluate` verdict on a dry intent for the sized quantity (never an order row), `engine.trading_halted`, the symbol's open-or-working count against `max_open_trades`, and the premium of working entries; snapshotted within 10 s of the evaluation; absent or stale = unknown | `technique/source_candidates_runtime.py` |
| `source-continuation-v1` / `requalification-v1` (clarified, same names, no data collected) | the authoritative source revision is chosen AS OF the evaluation (highest revision received by then; a tombstone has no scenarios; an older revision is never independently actionable); the plan, thresholds and profile are those of the plan available at the idea's BIRTH, never the latest plan; a pivot is knowable at the CLOSE of its confirming bar (`confirmedCloseTs`), and the invalidation at the close of the invalidating bar; irregular bars are held, a missing minute inside the structure window holds the child; the first persisted definition is immutable, a terminal candidate is never re-decided (only the after-the-fact outcome proxy is re-scored), one requalified child per BRANCH (note + symbol + direction + board position) per session; a superseded or deleted source closes only the FUTURE eligibility of its non-terminal candidates (`source_withdrawn`) | `technique/source_candidate_policy.py`, `requalification.py`, `source_candidates_runtime.py` |
| `first-sale-v2` (clarified) | derived, transformed or delayed underlier evidence is never an executable bound; `underlier-print-fallback-v1` is declared apart: a print needs its own fresh venue print time, is labelled `venue_print_fallback`, is never called an executable quote, and can only make admission stricter than the runner's entry | `technique/first_sale.py` |
| `em-prep-policy-v1` (clarified) | the decision identity also carries the effective plan geometry and reference price; one arm per candidate across workers and restarts (database advisory lock + a database re-check); under the proposed policy batch, manual, ingestion and pre-open arms pass the same owner; a human override is explicit and recorded | `technique/prep_service.py`, `technique/service.py` |
| `conditional-review-v1`, `capacity-v1`, `source-scenarios-v1`, `source-plan-match-v1`, `model-costs-v1`, frozen exception features | unchanged | as before |
