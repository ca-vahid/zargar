# 2026-09-21 11:04 ET - exception check (checker corrected first)

Read-only. No setting, pause or retune. Both books active, bundle frozen at `em-experiment-v1`.

## 1. The 07:22 check was wrong, and why

`em_experiment_check.exceptions()` reported "anything to report: **False**" at 07:22 while nine experimental entries had
already been deferred and three plan errors had been journaled. Two blind spots, both now fixed on the desk branch
(reporting tool only - it runs from the worktree, calls nothing in the engine, needs no deploy):

| Blind spot | What it did | Fix |
|---|---|---|
| Ownership decided on a nullable column | `TechniquePlanError` is journaled with `portfolio_id` NULL on paths that carry no book (a bars/quote failure). Those rows failed the `mine` test and were dropped as another desk's noise. | The run's book is resolved from `technique_armed` via the event's `aggregate_id`; the column is only one of the sources. |
| Refusals at the admission gate were invisible | Only halts, pauses, arm refusals and plan errors were counted. A plan that FIRED and was then deferred by first-sale produced no exception at all. | `TechniquePlanTriggerSkipped` rows whose `detail.disposition` starts with `deferred` or is `policy_error` are counted per book and per disposition, with symbols and first/last times. Rule-based refusals stay out: those are the method saying no. |

Re-run after the fix: **anything to report: True**, 3 faults, 9 admission deferrals.

## 2. What the corrected check reports now

| Item | Value |
|---|---|
| Faults | 3 x `TechniquePlanError` "stale bars" at 09:30:01 (2 experiment, 1 baseline) - one burst at the open, none since |
| Expected protective actions | 0 |
| Entries the admission gate could not decide | **9**, all in the experimental book, all `deferred_missing_evidence` |
| Deferral reason | `first-sale gate: required evidence is missing or invalid (underlier_invalid) - entry deferred, nothing sent` |
| Symbols deferred | IREN, ON, NBIS, SMH, AVGO, AMGN, MRVL, APP, QQQ (09:31:02 -> 10:17:02) |
| Halts / pauses / loss halts / arm refusals / quiesce | none |
| Order-rate rejections | 0 (busiest minute 4 orders against a cap of 30) |
| Recorder drops / unscorable captures | none |
| Restarts during the session | 0 |

## 3. Session state at 11:04 ET

| | EM Practice (baseline) | EM Experimental |
|---|---|---|
| Equity / day | 9,892.26 / **+42.66** | 9,849.60 / **0.00** |
| Fired / filled / skipped | 5 / 3 / 66 | 11 / **0** / 135 |
| Exits | stop 1, tp1 1 | none |
| Open positions | IREN calls x2, AVGO x9 | none |
| Working entries / pending exits / in-flight orders | 0 / 0 / 0 | same |

Engine v0.8.28 build `7ee5ad2a`, 143 armed, one engine pair, watchdog quiet, no restarts.

## 4. The open defect, unchanged

The host clock is still behind the database: **+8.5 s** at this check (+9.57 / +9.37 / +8.8 earlier). The Windows Time
service is not started. Every equity quote's `quoteTs` therefore looks future-dated to the engine, `validate_underlier`
returns `venue_time_in_future`, and first-sale defers the entry. That is the whole reason the experiment has zero fills
while the baseline trades normally - it is an environment fault, not a verdict on the method.

Two ways out, both still awaiting the user's decision; nothing has been changed:

1. Start / resync the Windows Time service. No code, no bundle change, no restart of the app. The gate would then pass.
2. Leave it and decide after the close whether the gate should tolerate a documented clock skew. Today's experimental
   result would then stand as "no fills for an environmental reason", not as a comparison.

## 5. Noted, not acted on

`tests/test_em_experiment.py::test_preparation_is_deterministic_duplicate_safe_and_independent_of_the_baseline_arm`
fails today and passes on other days. It is date-fragile, not a regression: the rig builds its synthetic market from
`weekdays(6)` relative to today, and this week's window produces a plan whose reward:risk is below 3.0, so the
deterministic rules refuse every trigger (`R2 reward:risk 0.57 / 1.95 / 0.86 to TP3 below 3.0`) and the test's
"1 eligible, 1 armed" assertion cannot hold. The rules behaved correctly; the fixture moved. The other 8 tests pass.
Left alone while the bundle is frozen - the fix is to pin the fixture geometry, not to change any rule.
