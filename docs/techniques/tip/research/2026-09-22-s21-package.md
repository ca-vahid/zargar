# Tips — September 21 review package (S21-01..08): completion packet

Prepared 2026-09-22 against [the comprehensive review](../reviews/2026-09-21-comprehensive-review.md) (main `7eb89303`,
live 0.8.28). The reviewer's four regressions are in `tests/test_sep21_economics_review.py` verbatim (they failed on that
head: fee 0.99 vs 1.04, null break-even, unmatched review counted as clean, human list cut at 20). All four pass now,
beside 23 pre-existing focused checks and 14 new ones in `tests/test_sep21_package.py`.

**Boundaries kept:** five-session observation period (gate `observe`, no enforcement), risk limits, stops, approval
controls, overnight and exit policies, source policy and the production model are unchanged. No cache pilot. Nothing
below activates a new execution policy or a cheaper route; those are prepared with their evidence and listed in §4.

## 1. Confirmed corrections (bugs) — built, tested

| id | defect | fix | tests |
|---|---|---|---|
| S21-01 | approval cards priced options with the commission alone (0.99) and printed null break-even/expiry although the OCC contract identified them | `approvals/proposals.py::_compute_risk_plan` uses `execcost.fees_from_settings` (commission + regulatory, the execution and analyst basis) and `_contract_metadata` (vehicle first, else the OCC symbol decoded, never guessed); strike/premium/type/DTE/expiry/hold reach `payoff_preview`; a payoff failure is logged and visible instead of swallowed; the card states that scenarios assume an exit AT the target | reviewer ×2, put control, missing-metadata control, decode helper |
| S21-02 | the observation report turned a missing/failed review into an empty opinion and "0 false negatives", printed only 20 of the human-review candidates, and the scheduled checkpoint counted any weekday decision, ignored exit codes and could write READY beside a failed report | `tip_review_gate_eval.prospective` joins run STATUS (complete / running / failed / unmatched / unevaluable), counts false negatives only on complete reviews, prints **Checkpoint eligibility: ELIGIBLE / INCOMPLETE**, lists every unresolved decision and exports ALL candidates with run ids, states, proposed-vs-applied management and full rationale; accounting-day anchors (`--until`). New `tools/tip_checkpoint_status.py` (pure, testable): observed = COMPLETED accounting day with observe decisions (today never counts), one cutoff for the count and every report, gate mode required, every report's exit code checked, atomic STATUS with READY / NOT-YET / INCOMPLETE / FAILED / INVALID, gaps printed for a human; `scripts/tips-five-session.ps1` rev 2 only launches it and propagates its exit code | reviewer ×2, incomplete fifth day, gap + mixed mode, command failure |
| S21-07a | a context-channel digest cut at `max_tokens` with invalid JSON died with no repair (run 10d44367) | `digest.py`: ONE bounded same-transcript repair (the analyst family's contract): partial answer kept in the transcript, "JSON only", only the repair turn gets 2× room (4000, not every call), usage of both attempts on the record, typed `failure` metadata preserved (`repaired: true` on success), no write happens before a valid parse so a repair can never duplicate a note | truncation→success, terminal truncation, cancellation during the repair |

## 2. Evidence built (diagnostics; no behaviour change)

- **S21-03 target-to-fill** — scorecard v4 "Target exits: touch → decision → order → fill": first touch on the 1m tape after entry, the manager's closed-bar decision time, order time and type, fill time/price, shortfall per unit and in dollars. VKTX 2026-09-21: touch 15:26, decided 15:30:00, filled 15:30:01 @ 29.904, shortfall **$11.83** on 17 sh. Stated on the table and on every card: arithmetic between the touched target and the realised fill, not evidence that a limit at the target would have filled. Design of the execution-policy comparison: [2026-09-21-target-execution-design.md](2026-09-21-target-execution-design.md) — prepared, not activated.
- **S21-04 all-in friction and exposure** — scorecard v4 "Friction and exposure of open positions": debit, entry fees paid, exit fees at the same basis, spread at the quote (role labelled), all-in $ and % of debit, hold cap, same-underlying other lots and cost. ACHR Sep-25 5.5C ×5: debit $70, all-in **$15.40 = 22.0%**, with the Jan-27 7C ×3 ($144) on the same underlying. Fill records now carry `decisionSampledAt`, `submittedAt` and an independently sampled `fillTimeQuote` (`quoteRole` decision / fill), so a stale decision comparison is never read as fill-time market quality. No friction threshold is applied.
- **S21-05 quote provenance (Tips side)** — every Tips quote record says which clock its age came from (`sourceTimeBasis` source / receipt / none, `receiptAgeSeconds`, `sourceAgeKnown`); execution-cost and fill records carry `quoteSourceTimeBasis`. A missing source time is never claimed. Finding for the shared owner: OPRA `source_ts` is stamped at the poll on this host (options/service.py), and equities carry no `source_ts` at all; `quote_ts` is populated only by the Alpaca feed, so "venue time or nothing" on the decision path would refuse every Yahoo-fed share entry — a user decision (§4), not a patch. The sim fill evidence already records `sourceAt: null` honestly.
- **S21-07b raw-message coverage + bounded replay** — `tip_outcomes --coverage`: every received message ends in one class (extracted with signals / extracted no signal / pending / failed / recovered / refused). 2026-09-21: 163 received, 66 with signals, 96 no signal, **1 failed** (MK-alpha-trades 12:32 ET, provider 529 twice; replayable). `POST /api/tip/intake/replay/{id}` + `tools/tip_intake_replay.py`: at most 2 attempts per message in total, marked before the work, journaled `TipIntakeReplayed`, re-enters the ordinary intake where stale content is replayed on history (never traded). Not run on the MK message: it is a context source in own-book observe; the replay is the user's call.
- **S21-08 source evidence** — `get_source_stats` now labels every shadow book (`bookKind`, `quarantined` + note, `feeParity`, `sampleSize`, `evidenceClass` research-only / invalid, `trustEvidence: false`, `limitation`) and blanks a quarantined book's P&L; trust is stated as closed Practice positions only. The two quarantined armed books (ab, eva) can no longer read as numbers. No promotion/demotion.
- **S21-06 context cost** — `tools/tip_review_context_cost.py` on the 135 captured reviews of 2026-09-21: the request header averages 77.8k chars, of which the **rulebook is 53.1k (68%)** and the shared notes 21.7k (28%); the message + outcomes are ~240 chars; recorded usage 128k input tokens per review over 3.2 turns (the header is re-sent per turn, plus tool results). Report: `economics/review-context-cost-2026-09-21.md`.

## 3. Reconciled economics (exact cutoff stated; the accounting day closes 03:59 ET)

Scorecard v4, Tips Practice, marks at the last persisted point (09-22 00:49 ET for the 09-21 row; the 03:59 mark will move it):

| window | marked change | realized net (method) | fees inside | questioned | priced model cost | **marked after cost (primary)** | realized after cost |
|---|---:|---:|---:|---:|---:|---:|---:|
| 2026-09-21 (session 1 of 5) | +2.15 | +16.63 | 4.16 | 0 | 122.50 | **−120.35** | −105.87 |
| 2026-09-08 → 09-21 cumulative | −1,033.39 | −922.48 | 116.48 | −97.41 | 638.68 (22 unpriced, 9 partial runs) | **−1,672.06** | −1,667.72 |

Cash-vs-executions and the equity identity reconcile to $0.00. Model cost is a list-price estimate and a lower bound.
2026-09-21 detail (EOD record): AAL +13.84 net (mirrored source exit), VKTX TP1 trim +2.79; six entries at or inside the
fresh ask; entry fees $7.28 inside the 8 open positions; held exposure ≈ 61% of equity, marked +6.06 over cost.

**Observation status:** the relevance filter journaled 134 decisions on 2026-09-21 (107 review $75.33 / 27 would-skip
$17.47, all 134 complete, 0 management false negatives, 27 human-review candidates exported). The gross would-save is
~19% of intake spend; observe still ran every review, so nothing was saved. `tip_checkpoint_status`: NOT-YET, 0 of 5
completed observe sessions (today is excluded until 04:00 ET). Intake coverage: 1 failed message, 0 pending.

## 4. Policy decisions (prepared, NOT activated) — one table

| # | decision | evidence | recommendation | default |
|---|---|---|---|---|
| P1 | enforce the relevance filter | 27/134 would-skip on day 1 ($17.47, 0 false negatives); all 27 carry watch/deferred content | wait for five complete sessions; human reads the exported list | observe |
| P2 | paid cheaper-review evaluation ($35 estimate-based guard) | 135 captured cases on day 1 exceed the 60-case quota by count; strata (20 management / 10 missed-entry / 8 correction / 6 mixed / 16 note-only) need ~4 sessions for the rare strata (`--model-plan`) | decide after the window; run only with approval and the limitation accepted | not run |
| P3 | compact review context | rulebook 68% + notes 28% of every review request, re-sent per turn | prepare the frozen comparison (pinned/core rules + ticker-scoped notes vs full) on the captured cases; no note deleted | full context |
| P4 | target execution: bar-touch/market vs quote-touch vs resting limit | VKTX shortfall $11.83 (one case); comparison design in the S21-03 note | collect the scorecard trail over the window; decide per technique in Practice with rollback | current policy |
| P5 | cheap-option friction | ACHR 22% all-in on a $70 debit; no threshold | keep as a card diagnostic; no hard reject from one trade; research lower-friction expressions at equal risk | no threshold |
| P6 | share-quote evidence rule | Yahoo shares have no venue time; "venue time or nothing" would refuse all Practice share entries | choose: label receipt-age (today) or require a venue-timed feed for share entries | receipt-age, labelled |
| P7 | replay the failed MK-alpha-trades message | 1 failed message, context source, own-book observe | user's call; tool ready, bounded to 2 attempts | not replayed |
| P8 | delayed-fill cards (ACHR rested 58 min) | fill-time quote now recorded apart | propose an expiry/cancel rule only on measured cases; never chase the limit | unchanged |
| P9 | portfolio concentration (≈61% of equity held) | exposure and same-underlying lots on the scorecard | report only; no size change | unchanged |
| P10 | consolidated pending rule 19777467 | born pending 09-19 | human read in Knowledge | non-operative |

## 5. Unresolved coverage and limits

- One session of prospective evidence; the five-session report stays owed and is produced by the checkpoint tooling.
- The 09-21 accounting day was not complete at this cutoff (00:49 ET mark); the 03:59 ET mark will restate the marked row.
- Model cost excludes stage-only extraction/transcription usage (unpriced) and is a lower bound.
- Shared-owner items handed over, not patched here: OPRA `source_ts` = host poll time; equity `quote_ts` only from Alpaca; the F33 budget-block journal gap (parked with Team2).
