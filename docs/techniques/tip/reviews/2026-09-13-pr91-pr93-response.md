# Response to the PR #91 / #93 readiness review — 2026-09-13/14

Reviewer note: `C:/Cursor/zargar-codex/docs/techniques/tip/reviews/2026-09-13-pr91-pr93-readiness.md`
(regressions in `pr91-pr93-regressions/`). All three regression files were adopted verbatim:
`tests/test_kb_0765_review.py`, `tests/test_geometry_pr91_review.py`,
`tests/test_integrity_pr93_review.py` — the 14 reproductions pass on each owning head and on
the combined tree.

## Commits

| Target | Head | What |
|---|---|---|
| Deployed knowledge fix | main `46af486` (PR #94, **v0.7.66 deployed 2026-09-13 ~22:5x ET**) | V65-KB-01 |
| PR #91 geometry | `31cb45a` (`3ffd6ce` fixes + `31cb45a` retro import) | G91-01..06 |
| PR #93 integrity pause | `2791f02` | I93-01..04 |
| Combined tree | PR #95 `claude/tips-geometry-integrity-integrated`, head `1f80bb9` (merge `0543de1` + docs `549f419` + geometry follow-up merge) | both gates, conflicts resolved |

Nothing activated: `techniques.tip.geometry_gate` = `shadow`, `techniques.tip.entry_pause_mode`
= `clock`. No live setting, knowledge, order or trading behaviour changed apart from the
v0.7.66 deploy of the knowledge fix.

## V65-KB-01 — fixed and deployed (v0.7.66)

`add_tip_note` now inserts the row and runs the rule-family classification
(`_classify_rule_family`, family rows locked; clean members superseded with a snapshot; the
newcomer set `needs_human=True` when a disputed member exists) inside ONE transaction; the
`TipNoteAdded` notification and the family journal entry follow the commit and may fail
without exposing an intermediate state; the returned note reflects the committed
classification. `test_kb_0765_review.py` passes; the earlier dispute/deletion tests are
unchanged and green.

## PR #91 — G91-01..06

| Finding | Status |
|---|---|
| G91-01 enforce admits missing/failed evidence | `_pre_entry_geometry` never raises into an advisory skip: under enforce a computation failure returns an enforced plan with `reviewRequired = "risk evidence unavailable: …"`; `_admit_geometry` refuses an automated entry without an enforced plan or with `reviewRequired`; `approve()` honours the refusal at the head AND at final submission (`_refuse_automated(..., revert=True)` returns the card to pending, no order); the stale-quote retry is admitted the same way; the armed-fire producer runs the same gate; spreads are review-gated under enforce (human only). Tests: reviewer `test_missing_enforced_risk_plan_cannot_auto_admit`; wiring `test_enforce_mode_computation_failure_is_review_gated_not_admitted`, `test_final_admission_refusal_reverts_an_automated_approval`. |
| G91-02 risk/protections differ from the submitted trade | Shares are sized at the BUY limit (`entryRefBasis: limit`); the delta's per-field age (`greeksFieldAsOf.delta`, else `asOf`) must be inside `techniques.tip.geometry_greeks_max_age_seconds` (900) and the underlying reference quote inside `geometry_quote_max_age_seconds` (300), non-delayed — otherwise NO estimate (review); the contract multiplier must be explicit on the vehicle (unknown = review, no 100 fallback); currency recorded; the share bracket is rebuilt from the FINAL plan (`_bracket_from_plan`) at creation and again at submission; `_admit_geometry` recomputes the WHOLE plan at the submitted limit (fresh reference, Greek age, geometry, budget) instead of premium arithmetic only. Tests: reviewer `test_stale_delta_cannot_authorize_enforced_risk`, `test_share_budget_uses_executable_limit_not_lower_last_trade`; wiring asserts the emitted bracket carries the final stop. |
| G91-03 Practice boundary | `_geometry_scope(pid)` gates creation, admission and the post-fill exception on `portfolio.kind == "sim"`; live/paper/shadow/unknown books never receive a plan. Tests: reviewer `test_practice_geometry_cannot_resize_live_book`; wiring `test_live_book_never_gets_a_plan_even_when_enforced`. |
| G91-04 duplicate-trim / abandoned reconcile | A durable `attemptId` is persisted before the trim is submitted; an existing unconsumed `geometry_trim` exit record is RECOVERED (adopted as this attempt's order) instead of a second trim; `reconcile` with a durable order id re-reads the order (`_await_trim`) and resolves; a still-working order after boot keeps being monitored; the residual is re-checked against the actual open legs before any widen. Tests: reviewer `test_filled_trim_before_geometry_stamp_must_not_be_submitted_again`, `test_timed_out_known_trim_is_reconciled_when_it_later_fills`; wiring trim-first + restart tests. |
| G91-05 shared widening trusts caller state | `widen_stop` runs under `position_guard` for every position, re-reads remaining quantity/status, refuses in-flight protective exits, over-`max_qty` and over-budget residuals, persists BEFORE exposing and reverts on persistence/journal failure. Test: wiring `test_widen_stop_refuses_stale_caller_state_and_reverts_on_journal_failure`. |
| G91-06 accounting unwired | `risk_accounting` reads `extras.riskPlan` / `config.extras.riskPlan`; `position_risk_accounting` is called by the analyst's positions tool (managed rows carry `riskAccounting`) and the retro's position summary. Test: wiring `test_accounting_is_read_from_serialized_positions`. |

## PR #93 — I93-01..04

| Finding | Status |
|---|---|
| I93-01 fails open / not every retry | `entry_paused` refuses on ANY store error; `list_incidents(limit=None)` for admission (complete open set); `admission()` runs `detect_incidents(strict=True)` first and refuses when detection is unavailable; `TipRunner._place_with_retry` owns its retry loop and re-admits immediately before every submission, transport retries included. Tests: reviewer `test_integrity_lookup_failure_cannot_admit_automatic_entry`, `test_transport_retry_rechecks_incident_before_second_order`. |
| I93-02 detector does not prove its claims | `_risk_plan_evidence` (enforced + `invariantOk is True` + a quote-freshness record, else missing; recorded violation/delayed quote = invalid); `_exit_evidence` needs a structured `confirmation` record with two observations (stamped by the position manager on premium-stop exits — prose never counts; `confirmed: false` = invalid); timing from the executions table (`_fill_times`), then `filledTs`, else "unknown"; a proven violation is invalid at any speed; duplicate executions are detected against the executions table; `record_pre_entry_failure` is wired from the geometry gate (combined tree) with an explicit `entryPath`; detection runs inside every admission. Tests: reviewer `test_missing_quote_and_budget_proof_are_not_a_valid_fast_loss`, `test_confirmation_word_is_not_structured_premium_exit_evidence`; acceptance classifier cases extended. |
| I93-03 release trusts assertions / calendar | `_resolve_proof` resolves `execution:` / `event:` / `position:` references to records (an API boolean is ignored); cause-specific validation at the examined revision; manager/store unavailable = unknown = not repaired; `repeated_pre_entry_failure` needs a successful post-incident pre-entry validation on the journal; `duplicate_execution` verified against executions; `shared_component` releases on resolved evidence or an explicit `override=True` with a substantive note, journaled and labeled as an override. Tests: reviewer `test_session_rollover_without_revalidation_does_not_release`, `test_unresolved_evidence_reference_does_not_validate_itself`; acceptance case 3 and 7 updated. |
| I93-04 cancellation ignores scope | `_cancel_resting_entries` applies `_scope_matches` (entry path + symbol) to proposals and passes `symbol` to the runner; a proposal-path incident never calls the armed cancel and vice versa; every cancel is verified at the order and a fill that won the race is journaled (`filled_before_cancel`). Test: reviewer `test_proposal_only_incident_does_not_cancel_armed_entries`; acceptance R1. |

## Combined tree (PR #95)

Conflicts resolved: `settings_service.py` (both knob blocks), `proposals.py` stale-quote retry
(integrity admission first, then the geometry plan at the retry limit); the auto-merged
duplicate `_refuse_automated` dropped; `approve()` asks both gates at the head and at final
submission; `record_pre_entry_failure` wired with `entryPath`.

Checks on the exact combined tree, sequential foreground chunks on `zargar_test_hubfix`:

- chunk 1 — 14 reproductions + `test_tip_geometry_rev2.py` + `test_tip_geometry_wiring.py` +
  `test_tip_integrity.py`: **50 passed**
- chunk 2 — `test_tip_runner.py`: **52 passed** (195.97 s)
- chunk 3 — `test_api_and_pipeline.py` + `test_recovery.py` + `test_signals_tip.py` +
  `test_platform_separation.py` + `test_desk.py`: **96 passed, 1 failed** on `0543de1` — the retro's
  `position_risk_accounting` import was missing (`test_retro_sweep_teaches_rules`); fixed on the geometry
  branch (`31cb45a`) and merged (`1f80bb9`), where that test passes with the 14 reproductions (15 passed)
- chunk 4 — `test_position_chaos.py` + `test_position_policies.py` +
  `test_premium_confirmation_review.py` + `test_tip_geometry.py` + the 20 adopted knowledge
  review files: **77 passed** (82.34 s; includes the re-run retro test)

All chunks ran as single sequential foreground pytest processes on the dedicated
`zargar_test_hubfix` database; no other desk's database or process was touched; the running
app (v0.7.66) was not restarted for any of the review-branch work.

## Final pass (PR #95 review of `df4a292`) — corrections C95-01..08

Reviewer note: `2026-09-13-pr95-final-pass.md` (regressions in `pr95-regressions/`, four files
adopted verbatim: `test_pr95_shared_followup.py`, `test_pr95_integrity_followup.py`,
`test_pr95_retry_followup.py`, `test_pr95_geometry_followup.py`).

| Finding | Correction |
|---|---|
| C95-01 confirmed premium stop wrote `confirmed:false` | `_confirm_premium_stop` FREEZES the accepted pair (`_premium_confirmed[p.id]`) the moment confirmation succeeds; `_premium_confirmation_record` consumes that immutable record for the exit receipt (never a rebuild from the pending state that is cleared next). The multi-leg contract is unchanged: no leg moves backward, at least one advances. Test: `test_successful_confirmation_retains_the_pair_for_exit_receipt` (real tick/watch path). |
| C95-02 shadow sizing reported as actual planned risk | `risk_accounting` carries provenance: `plannedRisk` = the EXECUTED trade's risk (`enforced-plan`: unit loss × actual quantity; `executed-plan`: entry, live stop, legs for shares) or `unavailable`; `slippageVsPlanned` only against that; an unenforced/shadow plan is reported under `hypothetical` (mode, qty, plannedRisk, resized) — research only. The analyst tool and the retro read this shape. Test: `test_shadow_resize_is_not_reported_as_actual_planned_risk` + updated acceptance cases. |
| C95-03 widening's helpers swallow failures | `widen_stop` uses a strict `_persist_candidate` (writes the row with the candidate policy/stop WITHOUT touching the in-memory position; raises) and a strict `engine.journal.append` (raises); the wider stop is exposed only after both; a durable write that fails leaves the tight stop in force, an unknown durable outcome is recorded explicitly in `extras.widenUncertain` (with a best-effort write-back of the tight stop) and repaired to the tight stop by `reconcile_geometry_exceptions` at restore. `serialized_adapter` now guards Tips positions too (reentrant per task), so fill/close/policy mutators share the guard. Tests: both parameters of `test_widen_does_not_swallow_real_persistence_or_journal_failure`; the wiring test now fails the REAL journal boundary. |
| C95-04 attempted trim without ACK re-submitted | The attempt identity is bound to the order intent: `close(attempt_tag=)` → `_close_leg` → `_submit_exit` puts `geometry_trim:<attemptId>` on the venue order's tags and the exit record. A persisted attempt with no local order id looks the order up by that tag (`_order_for_attempt`); when nothing can be found (or the store is unavailable) the exception HOLDS in `reconcile` with the tight stop and attention — never a second trim. A fresh submission that returns no local id is treated the same way. Test: `test_attempt_without_order_ack_is_not_submitted_again`. |
| C95-05 review-only spread reached `open_spread` | `_admit_geometry` applies the mandatory automated-review refusal BEFORE any vehicle dispatch; a vehicle the gate does not cover is refused for automated entries. Test: `test_unsupported_spread_review_card_cannot_auto_open`. |
| C95-06 unrelated evidence released a hold | `_resolve_proof(eng, e, inc)` binds every reference to the incident: an execution must be in the incident's book, for one of its symbols AND belong to one of its positions' orders (entry legs / exits); an event must be about one of its positions/proposals and in its book; a position proof must be one of its positions. Unrelated records are UNRESOLVED (never a release); only a bound record can prove a defect. Pre-entry success events for `repeated_pre_entry_failure` are bound to the book. Test: `test_unrelated_fill_cannot_release_a_missing_evidence_hold`. |
| C95-07 diagnostic receipt suppressed a missing incident | `detect_incidents` opens the required incident FIRST and journals the `TipFastStopDiagnostic` receipt (carrying `incidentId`) only afterwards — a failed incident write leaves the position unseen and is retried on the next detection. Test: `test_detection_retries_incident_after_diagnostic_commit`. |
| C95-08 quote retry checked the pause too early | The stale-quote retry re-runs `integrity.admission` immediately before the order, after the awaited geometry refresh, and builds the retry intent's bracket from the newly admitted proposal bracket (never a stale copy); once-only stamping and the never-raise limit are unchanged. Test: `test_incident_opened_during_geometry_refresh_blocks_quote_retry`. |
| Contract multiplier hardcoded 100 | `options/occ.contract_multiplier(symbol)`: the standard 100 only for a symbol that parses as a standard OCC contract; adjusted / unparseable contracts return `None`, which the vehicle builders now pass through and the geometry gate review-gates (`contract multiplier unknown`). |
| Shadow fixture stability | `test_shadow_mode_records_but_does_not_change_the_card` uses a one-cent risk budget (fits no share at any sim price) — deterministic shadow-invariance assertions. |

Final combined commit and check results: see the table below (appended after the runs).
