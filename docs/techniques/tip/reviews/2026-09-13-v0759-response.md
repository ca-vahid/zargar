# Team response — v0.7.59 reporting corrections + knowledgebase tags (2026-09-13)

Per your request, each tag reported as **fixed / staged / deferred /
policy decision**, with commit evidence in the release that carries this
file. "Fully closed out" is withdrawn: reporting was open, knowledge is a
program.

## C59 reporting — fixed

- **C59-01 fees**: three explicit fields — `fees paid` (matched inventory:
  allocated-to-realized + open-lot entry fees), `fees allocated to realized`,
  `open-lot entry fees`. T shows 18.72 paid / 8.32 allocated / 10.40 open; the
  live table: **92.56 paid = 79.04 allocated + 13.52 open-lot** — your two
  numbers, now summing on the page. Net realized unchanged (T +27.68;
  total −950.55). Unallocated-sell fees live only in the exceptions block.
- **C59-02 missing-lot sales**: the FIFO loop now iterates every in-scope
  SELL key (not just keys with lots); a sale with no recognized lot is a
  reconciliation exception with execution id, book, qty, proceeds, fee and
  reason (`no recognized lot in window (opening inventory unknown)` vs
  `oversold beyond recognized lots`) — no P&L invented. Deterministic
  tie-break `(ts, order_id, execution_id)` on both lots and sells. Scope is
  explicit: `--portfolio` or `techniques.tip.default_portfolio`, printed on
  the report ("Scope: portfolio 4611946d…"). Opening-inventory loading is
  deferred (no pre-window buys exist in this cohort; when they do, the
  exception says so instead of guessing basis).
- Your `test_v0759_reporting_review.py` adopted verbatim; 10 census tests green.
- Semantics corrections adopted: "complete price pairs (existence only)";
  `no-fill/missed` is a count, not a monetary counterfactual (header text).

## Knowledgebase tags

| tag | status | what shipped / what remains |
|---|---|---|
| KB-01 scheduling | **fixed** | `tip_knowledge_maintenance` is its own scheduler job (17:25 ET, `weekdays_only=False`), un-chained from the weekday-only retro. Runs the audits on `rule_audit_day` (America/New_York) OR as catch-up when no completion exists within 8 days. Every tick journals `TipKnowledgeMaintenance` with status `skipped / done / partial / failed`, due/overdue flags, last completion and per-audit sub-status — a quiet week is distinguishable from a broken one. Once-per-day + restart safety via the scheduler's journal hydration; batch idempotency (KB-02) covers a retry of an applied batch. Your Saturday reproduction passes. |
| KB-02 safe consolidation | **fixed** | `apply_knowledge_batch`: the whole proposed batch is validated first — any id in a contradiction is CONFLICT-LOCKED (flagged, never merged/expired; your contradictory-merge fixture leaves both unsuperseded + flagged, `merged=0, contradictions=2`); invalid/non-live ids are rejected with reasons on the journal, never dropped. Apply is ONE transaction with `FOR UPDATE` row locks and a version check (a concurrently superseded row aborts the whole batch, nothing half-written); durable idempotency by `batchId` (a retry after a commit-with-lost-ACK finds the journaled batch and no-ops). Audit-authored rules bypass the prose-family auto-supersede (`family_dedupe=False`) — the batch is the only apply path. Both the rule audit and every knowledge-group audit use it. |
| KB-03 historical integrity | **fixed (forward)** | `tip_note_revisions`: every mutation (edit, supersede, pin, TTL refresh, flag/resolve) snapshots the PRIOR state with `known_from/known_until`; the row carries `revised_at`/`revision_no`. An `as_of` read resolves the revision in force at that moment — your future-edit reproduction passes; a later pin/renewal/supersession cannot change the earlier view. **Legacy limitation, stated**: mutations before this release have no revision — such notes are EXCLUDED from historical views (counted, never backdated). Run snapshots now record `revisionNos` alongside `ruleIds`. |
| KB-04 complete retrieval | **staged** | Done: `/api/tip/notes/search` (paginated, total count) and the Knowledge tab shows "showing N of total" + load more (no silent 300 cut); the knowledge audit groups from a WHOLE-STORE count and fetches each group in full, with a per-run group budget that DEFERS visibly (`groupsEligible/Deferred/Failed` on the journal); rulebook selection is deterministic and manifested: CORE (pinned) rules always, then newest, `selection {total, core, recent, omitted}` on every run snapshot — adding case reports can no longer silently evict a pinned rule. **Remaining**: the compact canonical rulebook (core protections + relevant sections under a measured budget) is gated on KB-08 measurement, per your rule. The limit was NOT raised. |
| KB-05 dedupe by meaning | **staged (packet)** | `2026-09-13-consolidation-packet.md`: geometry family (32 → 1 canonical family + 14 evidence records, old→new mapping, rollback pointers), the four MuggZone/jon-and-kian pairs with what each merge must retain, the source-profile + evidence-record shape keyed by message id. NOT applied — waits for KB-06 decisions and goes through the KB-02 batch path. |
| KB-06 policy consistency | **policy decision required** | The kill-switch conflict (clock-based `dbfd8177` vs geometry-gated `7e72fd7f`; code implements clock) is laid out with the option that geometry rev 2 changes the question again. Both stay flagged/DISPUTED; the summarizer does not choose. Separation table (policy / hypothesis / engine defect / source interpretation / dated observation) in the packet. |
| KB-07 write hygiene | **fixed + staged** | One `normalize_scope` on every writer (analyst tool, API, digest, retro, experiment, edit): empty entity → refused; unknown prefix → refused; `ticker:` uppercased. Text is no longer sliced at 2,000 — a 20,000 ceiling refuses visibly. The 11 orphans: evidence-backed reassignment list in the packet, each marked "confirm" (no first-word inference), NOT applied. `16f86a3a`/`0600aecb`: promotion-provenance review item, no deletion. |
| KB-08 measurement | **staged** | Supplied vs relied-on are separate counters (`supplied_count`/`cited_count`) and the Knowledge UI now shows both with the honest label ("the model's own declaration, not proof it helped"); run snapshots record supplied rule ids + revision numbers + the selection manifest; per-turn input tokens (`inPerCall`) record on every run kind. **Deferred**: the frozen-evidence current-vs-compact comparison (grounding, protections, take/skip cases, no-verdict rate, p50/p95, tokens) — the gate for any change to supplied context. |

Bulk cleanup remains HELD, as instructed: safeguards first, decisions second,
one reviewed batch third.

## Also in this release

- Tips spread-test fixtures pinned an expiry of 2026-09-11 and rotted on the
  12th; they derive a ~45-day expiry now.
- Runtime note: your health check hit the 16:05 PT restart window (another
  desk's v0.7.60 deploy); the app was up seconds later.
